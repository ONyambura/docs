import { Callout } from 'nextra/components';

## Versions

### v1.0.0 (Current - Stable)

<Callout type="info">
Initial release.
</Callout>

- In-game sling editor (NUI panel + 3D gizmo).
- First-run setup wizard (inventory, image path, theme).
- JSON or database save (`Config.Save`).
- Client / server exports to hide and respawn props.
